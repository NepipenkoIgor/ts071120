enum Size {
    S = 22,
    M = 44,
    XL = 52
};

enum Tooltip {
    TOP = 'TOP ',
    LEFT = 'LEFT',
    BOTTOM = 'BOTTOM',
    RIGHT = 'RIGHT'
};

enum UserActions {
    CREATE = '[USER MODULE] Create user ',
    DELETE = '[USER MODULE] DELETE user ',
    REMOVE = DELETE
};

let mySize = Size.M;

let position = Tooltip.LEFT;
